DATOCMS_PUBLISHED_CONTENT_CDA_TOKEN=
DATOCMS_DRAFT_CONTENT_CDA_TOKEN=
DATOCMS_CMA_TOKEN=
DATOCMS_BASE_EDITING_URL=
SECRET_API_TOKEN=

# Canonical site origin, used for metadataBase, the sitemap and robots.txt.
# Falls back to the Vercel production alias, then to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=

# Set to `false` while the domain still points at the old website: robots.txt
# then disallows everything. Anything other than the literal `false` is live.
SITE_LIVE=
