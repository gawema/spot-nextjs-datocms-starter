export const metadata = {
  title: 'Home | SPOT Starter Next',
};

/**
 * Placeholder home page. It becomes CMS-driven together with the page/sections
 * spine, which renders the `page` record whose slug is `home`.
 */
export default function Page() {
  return <h2>SPOT starter</h2>;
}
